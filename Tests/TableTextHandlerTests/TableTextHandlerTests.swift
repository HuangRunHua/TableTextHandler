import XCTest
@testable import TableTextHandler

final class TableTextHandlerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TableTextHandler().text, "Hello, World!")
    }
}
